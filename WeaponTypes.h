//This Header defines all weapons in the game
//Data is passed via pointers and references

/*
#ifndef _WEAPONTYPE_H
#define _WEAPONTYPE_H

#include "WeaponsClass.h"


void WeaponsClasses()
{
	Weapons AR;
	AR.SetStats(10, rand() % 10 , rand() % 15 + 2);
	AR.SetName("M-17A1");
	*ARC->AR.nClip;
	*ARA->AR.nAccurate;
	*ARD->AR.nDamage;
	*ARN->AR.cStrName;

	Weapons SR;
	SR.SetStats(1, rand() % 15 , rand() % 35 + 2);
	SR.SetName("Solid Rifle");
	*SRS = SR.SetStats;
	*SRN = SR.SetName;

	Weapons SG;
	SG.SetStats(1, rand() % 15, rand() % 10 + 5);
	SG.SetName("SPAS-10");
	&SGS = SG.SetStats;
	&SGN = SG.SetName;
	

};

#endif
*/